# Pinned versions, verified working together on Python 3.12 (2026-07-15).
fastapi==0.139.0
uvicorn==0.51.0
httpx==0.28.1
presidio-analyzer==2.2.363
presidio-anonymizer==2.2.363
spacy==3.8.13
pytest==9.1.1

# spaCy English model used by the Presidio analyzer. Installed as a direct
# wheel URL so `pip install -r requirements.txt` is sufficient on its own
# (equivalent to `python -m spacy download en_core_web_sm`).
en_core_web_sm @ https://github.com/explosion/spacy-models/releases/download/en_core_web_sm-3.8.0/en_core_web_sm-3.8.0-py3-none-any.whl
